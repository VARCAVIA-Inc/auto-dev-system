project_name: VARCAVIA Office - MVP Test
description: "Missione di test end-to-end per validare il ciclo completo."

phases:
  - name: "Fase 1: Validazione del Ciclo Completo"
    tasks:
      - description: "Creare un nuovo modulo in `src/utils/math_utils.py` che contenga una funzione `add(a, b)` che restituisce la somma di due numeri. Creare anche un file di test `tests/utils/test_math_utils.py` che verifichi il corretto funzionamento della funzione `add`."
        agent: ManagerBot
        status: pending