version: v1
name: buf.build/varcavia/sentient-organism

deps:
  # Include gli stub Google standard (solo se effettivamente importi *.proto di Google)
  - buf.build/googleapis/googleapis

  # gRPC-Gateway contiene già i protos interni + plugin swagger
  - buf.build/grpc-ecosystem/grpc-gateway

breaking:
  use:
    - FILE

lint:
  use:
    - DEFAULT
