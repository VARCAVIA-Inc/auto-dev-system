services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  state_aggregator:
    # costruiamo l’immagine al volo partendo dalla root del repo
    build:
      context: ../..                         # <- rispetto a test/integration
      dockerfile: services/state_aggregator/Dockerfile
    environment:
      - REDIS_ADDR=redis:6379
    depends_on:
      - redis
    ports:
      - "50051:50051"
